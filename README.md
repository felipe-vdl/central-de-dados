# central-de-dados
# With Docker:
- cp .env.example .env
- Configure API Credentials
- docker compose up

# Without Docker:
- cp .env.example .env
- Configure API Credentials
- npm run build
- npm run preview
